# Re.form CRM — зависимости
# База данных теперь SQLite (модуль sqlite3 встроен в Python — ставить нечего).

aiogram==3.7.0          # Telegram-бот
flask==3.0.3            # веб-панель
Pillow==10.4.0          # карточка профиля
requests==2.32.3        # отправка в Telegram из веб-панели
aiohttp==3.9.5          # интеграция с YClients

# Десктоп-приложение
pywebview==5.2          # нативное окно вокруг веб-панели
